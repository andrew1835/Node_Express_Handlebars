<h1>Eat Your Burgers!</h1>

<h2>Burgers you've eaten!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block eaten=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that you haven't eaten!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block eaten=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Have you eaten it yet?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Eaten!<br>
    <input type="radio" name="sleepy" value="0"> Not Eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>