<h1>Burgers!</h1>

<h2>Burgers ready to be eaten</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you have eaten</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers devoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Type of Burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Hungry?</label><br>
    <input type="radio" name="devoured" value="1" checked> Burger has been devoured!<br>
    <input type="radio" name="devoured" value="0"> Ready to be eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>